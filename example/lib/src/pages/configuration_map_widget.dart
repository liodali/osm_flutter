import 'package:flutter/widgets.dart';

class ConfigurationMapWidget extends StatelessWidget {
  const ConfigurationMapWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
